{% extends 'base.html.twig' %}

{% block content %}
<body>
<div id="container" class="boxshadow" style="border-radius:5px; margin:3rem; background-color:#F9F9F9;">
    <div id="welcome">
        <h1 class="text-center text-primary" style="font-family: 'Bitter', serif;">————— SF-Quiz —————</h1>
        <p style="font-size:13px;">Des quiz fun auxquels tout le monde peut participer ?
            C'est ça Easy Quiz. Parce que vous aimeriez mieux vous connaitre au fond,
            on le sait bien. Easy QUiz, c'est moins cher qu'une scéance de psy.
            Ok c'est aussi moins efficace. Mais c'est moins cher.</p>
        <hr />
    </div>
    <div class="row">
        <div class="card-columns">
            {% for categorie in data %}
                <div class="card text-center boxshadow">
                    <img class="card-img-top" src="{{ categorie.image }}" alt="Card image">
                    <div class="card-footer text-dark"style="border:none; background-color:transparent;">
                        <h5 style="font-family: 'Bitter', serif;">{{ categorie.name }}</h5>
                        <hr />
                        <a href="#" class="btn btn-outline-primary">Commencer</a>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
    <hr  class="">
    <nav aria-label="Page navigation example">
        <ul class="pagination justify-content-center">
            <li class="page-item">
                <a class="page-link" href="#" aria-label="Previous">
                    <span aria-hidden="true">&laquo;</span>
                    <span class="sr-only">Previous</span>
                </a>
            </li>
            <li class="page-item"><a class="page-link" href="#">1</a></li>
            <li class="page-item"><a class="page-link" href="#">2</a></li>
            <li class="page-item"><a class="page-link" href="#">3</a></li>
            <li class="page-item">
                <a class="page-link" href="#" aria-label="Next">
                    <span aria-hidden="true">&raquo;</span>
                    <span class="sr-only">Next</span>
                </a>
            </li>
        </ul>
    </nav>
</div>
</div>
</body>
{% endblock %}

{% block stylesheets %}
    <style>
        body: { background-color: #D9DCDE; }
        h1, h2 { line-height: 1.2; margin: 0 0 .5em; }
        h1 { font-size: 36px; }
        h2 { font-size: 21px; margin-bottom: 1em; }
        h5 { font-size: 1.10rem; }
        p { margin: 0 0 1em 0; }
        a:hover { text-decoration: none; }
        code { background: #F5F5F5; max-width: 100px; padding: 2px 6px; word-wrap: break-word; }
        #wrapper { background: #FFF; margin: 1em auto; max-width: 1000px; width: 100%; border-radius:5px; }
        #container { padding: 2em; }
        #welcome, #status { margin-bottom: 2em; }
        #welcome h1 span { display: block; font-size: 75%; }
        #icon-status, #icon-book { float: left; height: 64px; margin-right: 1em; margin-top: -4px; width: 64px; }
        #icon-book { display: none; }
        .boxshadow { box-shadow: 0 1px 5px rgba(0, 0, 0, 0.46); }

        @media (min-width: 768px) {
            #wrapper { width: 80%; margin: 2em auto; }
            #icon-book { display: inline-block; }
            #status a, #next a { display: block; }

            @-webkit-keyframes fade-in { 0% { opacity: 0; } 100% { opacity: 1; } }
            @keyframes fade-in { 0% { opacity: 0; } 100% { opacity: 1; } }
            .sf-toolbar { opacity: 0; -webkit-animation: fade-in 1s .2s forwards; animation: fade-in 1s .2s forwards;}
        }
    </style>
{% endblock %}
